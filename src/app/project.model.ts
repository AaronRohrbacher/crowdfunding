export class Project {
  constructor (public name: string, public creators: string, public description: string, public fundGoal: number, public doWithFunds: string, public perks: string, public category: string, public type: string ) { }
}
